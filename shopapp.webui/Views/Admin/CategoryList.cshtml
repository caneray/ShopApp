@* @model CategoryListViewModel

<div class="row">
    <div class="col-md-12">
        <h1 class="h3">Admin Categories</h1>
        <hr>
        <a class="btn btn-primary btn-sm" href="/admin/categories/create">Add Category</a>
        <table class="table table-bordered mt-3">
            <thead>
                <tr>
                    <td style="width: 30px;">Id</td>
                    <td>Name</td>
                    <td style="width: 150px;"></td>
                </tr>
            </thead>
            <tbody>
                @if(Model.Categories.Count>0)
                {
                       @foreach (var item in Model.Categories)
                        {
                            <tr>
                                <td>@item.CategoryId</td>
                                <td>@item.Name</td>  
                                <td>
                                    <a href="/admin/categories/@item.CategoryId" class="btn btn-primary btn-sm mr-2">Edit</a>
                                    
                                    <form action="/admin/deletecategory" method="POST" style="display: inline;">
                                        <input type="hidden" name="categoryId" value="@item.CategoryId">
                                        <button type="submit" class="btn btn-danger btn-sm">Delete</button>
                                    </form>
                                </td>                        </tr>
                        }
                }else{
                    <div class="alert alert-warning">
                        <h3>No Categories</h3>
                    </div>
                }

             
            </tbody>
        </table>
    </div>
</div> *@



@model CategoryListViewModel

<div class="container mt-5">
    <div class="card">
        <div class="card-header bg-dark text-white text-center">
            <h2>Kategori Listesi</h2>
        </div>
        <div class="card-body">
            <div class="d-flex justify-content-between align-items-center mb-3">
                <a class="btn btn-info btn-sm mr-2" href="/admin/categories/create">Kategori Ekle</a>
            </div>
            <div class="table-responsive">
                <table class="table table-bordered table-hover">
                    <thead class="table-dark">
                        <tr>
                            <th style="width: 30px;">Id</th>
                            <th>Adı</th>
                            <th style="width: 150px;">İşlemler</th>
                        </tr>
                    </thead>
                    <tbody>
                        @if (Model.Categories.Count > 0)
                        {
                            @foreach (var item in Model.Categories)
                            {
                                <tr>
                                    <td>@item.CategoryId</td>
                                    <td>@item.Name</td>
                                    <td>
                                        <a href="/admin/categories/@item.CategoryId" class="btn btn-info btn-sm mr-2">Düzenle</a>
                                        <form action="/admin/deletecategory" method="POST" class="d-inline">
                                            <input type="hidden" name="categoryId" value="@item.CategoryId">
                                            <button type="submit" class="btn btn-danger btn-sm">Sil</button>
                                        </form>
                                    </td>
                                </tr>
                            }
                        }
                        else
                        {
                            <tr>
                                <td colspan="3" class="text-center">
                                    <div class="alert alert-warning">
                                        <h4 class="alert-heading">Kategori Yok</h4>
                                        <p>
                                            Kullanılabilir kategori yok. Lütfen yeni bir kategori ekleyin.
                                        </p>
                                    </div>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>
